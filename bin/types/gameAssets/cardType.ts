export type cardType = {
    color: string
    pic_url: string,
    type: string,
    amount?: number,
    value: number
}

// Types: Money, property, action.
// Colors: black, blue, brown, cyan, green, orange, pink, red, teal, yellow.